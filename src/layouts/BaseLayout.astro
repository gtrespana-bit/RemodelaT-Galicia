---
import '../styles/global.css';
import Analytics from '../components/Analytics.astro';
import SEO from '../components/SEO.astro';
import { BRAND, COPY, waLink } from '../data/brand';

interface Props {
  title: string;
  description: string;
  image?: string;
  activePage?: 'inicio' | 'servicios' | 'metodo' | 'proyectos' | 'nosotros' | 'blog' | 'contacto';
  articleType?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  author?: string;
  articleSection?: string;
  tags?: string[];
  noindex?: boolean;
}

const {
  title,
  description,
  image = '',
  activePage = '',
  articleType = 'website',
  publishedTime,
  modifiedTime,
  author,
  articleSection,
  tags,
  noindex = false
} = Astro.props;

const currentPath = Astro.url.pathname;

/* Páginas que arrancan con fondo claro: la barra transparente con texto blanco
   quedaba invisible sobre blanco hasta hacer scroll. Ahí se pinta sólida desde el inicio. */
const lightHeaderRoutes = ['/privacidad', '/cookies', '/aviso-legal', '/404'];
const normalizedPath = currentPath.replace(/\/$/, '') || '/';
const needsSolidNav = lightHeaderRoutes.includes(normalizedPath);
const isHomePage = normalizedPath === '/';

const whatsappText = 'Hola, quiero solicitar una valoración técnica para mi proyecto de reforma';
const whatsappHref = waLink(whatsappText);

const footerDesc = COPY.es.footerDesc;
const footerRights = 'Todos los derechos reservados.';
const footerLocations = BRAND.areas;

const navItems = [
  { label: 'Inicio', href: '/', key: 'inicio' },
  { label: 'Servicios', href: '/servicios/', key: 'servicios' },
  { label: 'Método', href: '/metodo-remodelat/', key: 'metodo' },
  { label: 'Proyectos', href: '/proyectos/', key: 'proyectos' },
  { label: 'Nosotros', href: '/sobre-nosotros/', key: 'nosotros' },
  { label: 'Blog', href: '/blog/', key: 'blog' },
  { label: 'Contacto', href: '/contacto/', key: 'contacto', premium: true }
];

const footerServices = [
  { label: 'Reforma Integral', href: '/servicios/reforma-integral/' },
  { label: 'Reforma de Cocinas', href: '/servicios/reforma-cocina/' },
  { label: 'Reforma de Baños', href: '/servicios/reforma-bano/' },
  { label: 'Reforma de Viviendas', href: '/servicios/reforma-vivienda/' },
  { label: 'Adaptación y Accesibilidad', href: '/servicios/reforma-accesibilidad/' },
  { label: 'Fontanería', href: '/servicios/fontaneria/' },
];

const footerZones = [
  { label: 'A Coruña', href: '/reforma-integral-a-coruna/' },
  { label: 'Oleiros', href: '/reforma-integral-oleiros/' },
  { label: 'Culleredo', href: '/reforma-integral-culleredo/' },
  { label: 'Cambre', href: '/reforma-integral-cambre/' },
  { label: 'Arteixo', href: '/reforma-integral-arteixo/' },
  { label: 'Sada', href: '/reforma-integral-sada/' },
  { label: 'Bergondo', href: '/reforma-integral-bergondo/' },
  { label: 'Betanzos', href: '/reforma-integral-betanzos/' },
  { label: 'Carral', href: '/reforma-integral-carral/' },
  { label: 'Abegondo', href: '/reforma-integral-abegondo/' },
];
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  {isHomePage && (
    <>
      <link rel="preload" href="/fonts/cormorant-garamond-v21-latin-400.v2.woff2" as="font" type="font/woff2" crossorigin="anonymous" />
      <link rel="preload" href="/fonts/cormorant-garamond-v21-latin-600.v2.woff2" as="font" type="font/woff2" crossorigin="anonymous" />
      <link rel="preload" href="/fonts/manrope-v15-latin-400.v2.woff2" as="font" type="font/woff2" crossorigin="anonymous" />
      <link rel="preload" href="/fonts/manrope-v15-latin-600.v2.woff2" as="font" type="font/woff2" crossorigin="anonymous" />
    </>
  )}

  <SEO
    title={title}
    description={description}
    image={image}
    type={articleType}
    publishedTime={publishedTime}
    modifiedTime={modifiedTime}
    author={author}
    articleSection={articleSection}
    tags={tags}
    activePage={activePage}
    noindex={noindex}
  />

  <Analytics />
</head>

<body>
  <div class="god-progress" id="god-progress" aria-hidden="true"></div>
  <a href="#main-content" class="skip-link">Saltar al contenido principal</a>

  <nav id="navbar" class={`nav${needsSolidNav ? ' nav-solid' : ''}`} role="navigation" aria-label="Menú principal">
    <div class="nav-container">
      <a href="/" class="logo">
        <img
          src="/images/logo-header-280.webp"
          srcset="/images/logo-header-180.webp 180w, /images/logo-header-280.webp 280w, /images/logo-header-560.webp 560w"
          sizes="(max-width: 480px) 182px, (max-width: 768px) 200px, 283px"
          alt="RemodelaT Coruña"
          width="280"
          height="75"
          loading="eager"
          decoding="async"
        />
      </a>

      <ul class="nav-links" id="primary-nav">
        {navItems.map((item) => (
          <li>
            <a
              href={item.href}
              class={[
                activePage === item.key ? 'active' : '',
                item.premium ? 'btn-nav-premium' : ''
              ].filter(Boolean).join(' ')}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>

      <button id="menu-btn" class="menu-btn" aria-label="Abrir menú de navegación" aria-expanded="false" aria-controls="primary-nav">
        <svg class="icon-open" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <line x1="3" y1="12" x2="21" y2="12"></line>
          <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
        <svg class="icon-close" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
          <line x1="6" y1="6" x2="18" y2="18"></line>
          <line x1="18" y1="6" x2="6" y2="18"></line>
        </svg>
      </button>
    </div>
  </nav>

  <div class="nav-overlay" aria-hidden="true"></div>

  <main id="main-content">
    <slot />
  </main>

  <footer class="footer">
    <div class="container">
      <div class="footer-grid">
        <div class="footer-col footer-brand">
          <div class="footer-logo">
            <img src="/images/logo-footer.webp" alt="RemodelaT Coruña" width="714" height="192" loading="lazy" decoding="async" />
          </div>
          <p class="footer-desc">{footerDesc}</p>
        </div>

        <div class="footer-col">
          <h3 class="footer-heading">Servicios</h3>
          <ul class="footer-links">
            {footerServices.map((l) => (
              <li><a href={l.href}>{l.label}</a></li>
            ))}
          </ul>
        </div>

        <div class="footer-col">
          <h3 class="footer-heading">Zonas de Servicio</h3>
          <ul class="footer-links footer-links-2col">
            {footerZones.map((l) => (
              <li><a href={l.href}>{l.label}</a></li>
            ))}
          </ul>
        </div>

        <div class="footer-col">
          <h3 class="footer-heading">Empresa</h3>
          <ul class="footer-links">
            <li><a href="/">Inicio</a></li>
            <li><a href="/sobre-nosotros/">Sobre Nosotros</a></li>
            <li><a href="/metodo-remodelat/">Método</a></li>
            <li><a href="/proyectos/">Proyectos</a></li>
            <li><a href="/blog/">Blog</a></li>
            <li><a href="/contacto/">Contacto</a></li>
            <li><a href={whatsappHref} target="_blank" rel="noopener">WhatsApp</a></li>
            <li><a href={`mailto:${BRAND.email}`}>{BRAND.email}</a></li>
          </ul>
        </div>
      </div>

      <div class="footer-bottom">
        <p>&copy; {new Date().getFullYear()} {BRAND.name}. {footerRights} | {footerLocations}</p>
        <ul class="footer-legal">
          <li><a href="/aviso-legal/">Aviso Legal</a></li>
          <li><a href="/privacidad/">Política de Privacidad</a></li>
          <li><a href="/cookies/">Política de Cookies</a></li>
        </ul>
      </div>
    </div>
  </footer>

  <a href={whatsappHref} target="_blank" rel="noopener" class="whatsapp-float" aria-label="Contactar por WhatsApp">
    <svg viewBox="0 0 24 24" fill="white" width="28" height="28"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
  </a>

  <div class="mobile-cta-bar">
    <a href={`tel:${BRAND.phone}`} class="mobile-cta-btn mobile-cta-call" aria-label="Llamar">
      <svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20"><path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/></svg>
      <span>Llamar</span>
    </a>
    <a href={whatsappHref} target="_blank" rel="noopener" class="mobile-cta-btn mobile-cta-wa" aria-label="WhatsApp">
      <svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
      <span>WhatsApp</span>
    </a>
  </div>

  <script>
    const navbar = document.getElementById('navbar');
    if (navbar) {
      let isScrolled = window.scrollY > 80;
      navbar.classList.toggle('scrolled', isScrolled);

      let navTicking = false;
      const syncNavbarState = () => {
        const nextScrolled = window.scrollY > 80;
        if (nextScrolled !== isScrolled) {
          navbar.classList.toggle('scrolled', nextScrolled);
          isScrolled = nextScrolled;
        }
        navTicking = false;
      };

      window.addEventListener('scroll', () => {
        if (navTicking) return;
        navTicking = true;
        window.requestAnimationFrame(syncNavbarState);
      }, { passive: true });
    }

    const menuBtn = document.getElementById('menu-btn');
    const navLinks = document.querySelector('.nav-links');
    const navOverlay = document.querySelector('.nav-overlay');
    if (menuBtn && navLinks) {
      const closeMenu = (restoreFocus = false) => {
        document.body.classList.remove('nav-open');
        menuBtn.setAttribute('aria-expanded', 'false');
        if (restoreFocus) menuBtn.focus({ preventScroll: true });
      };
      const toggleMenu = () => {
        const open = document.body.classList.toggle('nav-open');
        menuBtn.setAttribute('aria-expanded', open ? 'true' : 'false');
        if (open) navLinks.scrollTop = 0;
      };
      menuBtn.addEventListener('click', toggleMenu);
      navLinks.querySelectorAll('a').forEach((a) => {
        a.addEventListener('click', () => closeMenu());
      });
      navOverlay?.addEventListener('click', () => closeMenu());
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && document.body.classList.contains('nav-open')) closeMenu();
      });
    }
  </script>
</body>
</html>

<style>
  .skip-link {
    position: absolute; left: -9999px; top: auto; width: 1px; height: 1px;
    overflow: hidden; z-index: 9999; background: #c9a961; color: #0f2233;
    padding: 12px 24px; font-weight: 600; font-size: 0.95rem;
    text-decoration: none; border-radius: 0 0 6px 0;
  }
  .skip-link:focus { position: fixed; left: 0; top: 0; width: auto; height: auto; clip: auto; }
  *:focus-visible { outline: 2px solid #C9A961; outline-offset: 2px; }
  button:focus-visible, a:focus-visible { outline: 2px solid #C9A961; outline-offset: 2px; }

  .nav {
    position: fixed; top: 0; left: 0; width: 100%; background: transparent; z-index: 1000;
    padding: calc(22px + env(safe-area-inset-top)) 0 22px;
    transition: background 0.45s var(--ease-god, cubic-bezier(.16,1,.3,1)), padding 0.45s var(--ease-god, cubic-bezier(.16,1,.3,1)), backdrop-filter 0.45s;
    backdrop-filter: blur(18px) saturate(1.12);
    border-bottom: 1px solid rgba(201,169,97,0.0);
  }
  .nav.scrolled, .nav-solid {
    background: rgba(15,34,51,0.94);
    padding: calc(14px + env(safe-area-inset-top)) 0 14px;
    border-bottom: 1px solid rgba(201,169,97,0.22);
    backdrop-filter: blur(18px) saturate(1.2);
  }
  .nav-solid .logo img { height: 63px; }
  .nav-container {
    display: flex; justify-content: space-between; align-items: center;
    max-width: 1200px; margin: 0 auto;
    padding: 0 max(20px, env(safe-area-inset-right)) 0 max(20px, env(safe-area-inset-left));
  }
  .logo { display: flex; align-items: center; }
  .logo img { height: 76px; width: auto; display: block; transition: height 0.3s; }
  .nav.scrolled .logo img { height: 63px; }
  .nav-links { display: flex; gap: 32px; list-style: none; align-items: center; }
  .nav-links a { color: rgba(255,255,255,0.88); font-size: 0.80rem; text-transform: uppercase; letter-spacing: 0.12em; transition: color 0.3s; position: relative; font-weight: 500; }
  .nav-links a:hover, .nav-links a.active { color: var(--gold); }
  .nav-links a.active::after { content: ''; position: absolute; bottom: -5px; left: 0; width: 100%; height: 1px; background: var(--gold); }
  .btn-nav-premium { border: 1px solid var(--gold); padding: 8px 22px; border-radius: var(--r-sm); color: var(--gold) !important; font-weight: 500; }
  .btn-nav-premium:hover { background: var(--gold); color: var(--bg) !important; }

  .menu-btn { display: none; align-items: center; justify-content: center; min-width: 44px; min-height: 44px; background: none; border: none; color: var(--white); cursor: pointer; padding: 5px; margin-left: 8px; }
  .nav-overlay { display: none; }
  .menu-btn svg { width: 28px; height: 28px; display: block; }
  .menu-btn .icon-close { display: none; }
  body.nav-open .menu-btn .icon-open { display: none; }
  body.nav-open .menu-btn .icon-close { display: block; color: var(--gold); }

  .whatsapp-float { position: fixed; bottom: 25px; right: 25px; width: 60px; height: 60px; background: #25D366; border-radius: 50%; display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4); transition: all 0.3s; z-index: 99; }
  .whatsapp-float:hover { transform: scale(1.1); box-shadow: 0 6px 25px rgba(37, 211, 102, 0.6); }

  .footer { background: var(--bg); color: rgba(255,255,255,0.7); padding: 70px 0 35px; }
  .footer-grid { display: grid; grid-template-columns: 1.4fr 1fr 1fr 1fr; gap: 40px; margin-bottom: 45px; text-align: left; }
  .footer-brand { text-align: center; }
  .footer-logo { margin-bottom: 20px; display: flex; justify-content: center; }
  .footer-logo img { max-width: 320px; width: 100%; height: auto; display: block; aspect-ratio: 714 / 192; }
  .footer-desc { max-width: 340px; margin: 0 auto; font-size: 0.95rem; line-height: 1.6; }
  .footer-heading { color: #fff; font-size: 1rem; font-weight: 700; margin-bottom: 16px; text-transform: uppercase; letter-spacing: 0.05em; }
  .footer-links { list-style: none; padding: 0; margin: 0; }
  .footer-links li { margin-bottom: 9px; }
  .footer-links a { color: rgba(255,255,255,0.65); text-decoration: none; font-size: 0.9rem; transition: color 0.2s; }
  .footer-links a:hover { color: var(--gold); }
  .footer-links-2col { column-count: 2; column-gap: 24px; }
  .footer-links-2col li { break-inside: avoid; }
  .footer-bottom { border-top: 1px solid rgba(255,255,255,0.12); padding-top: 24px; font-size: 0.85rem; color: rgba(255,255,255,0.5); text-align: center; display: flex; flex-direction: column; align-items: center; gap: 12px; }
  .footer-bottom p { margin: 0; }
  .footer-legal { list-style: none; padding: 0; margin: 0; display: flex; gap: 20px; flex-wrap: wrap; justify-content: center; }
  .footer-legal a { color: rgba(255,255,255,0.55); text-decoration: none; transition: color var(--dur-fast) var(--ease-out); }
  .footer-legal a:hover { color: var(--gold); }

  @media (max-width: 900px) {
    .nav.scrolled,
    .nav-solid { backdrop-filter: blur(18px) saturate(1.2); }
    .nav-links {
      display: flex;
      position: fixed;
      inset: 0 0 auto auto;
      width: min(360px, 86vw);
      height: 100vh;
      height: 100dvh;
      flex-direction: column;
      justify-content: flex-start;
      align-items: flex-start;
      gap: 28px;
      padding:
        calc(96px + env(safe-area-inset-top))
        max(32px, env(safe-area-inset-right))
        calc(40px + env(safe-area-inset-bottom))
        32px;
      background: rgba(15,34,51,.98);
      backdrop-filter: blur(18px) saturate(1.15);
      border-left: 1px solid rgba(201,169,97,.28);
      box-shadow: -28px 0 70px rgba(0,0,0,.5);
      transform: translateX(100%);
      transition: transform var(--dur-base) var(--ease-out);
      z-index: 2;
      overflow-y: auto;
      overscroll-behavior: contain;
    }
    body.nav-open .nav-links { transform: translateX(0); }
    .nav-links a { font-size: 1.05rem; letter-spacing: .10em; font-weight: 500; }
    .nav-links a.active::after { bottom: -6px; height: 1px; }
    .nav-links .btn-nav-premium { margin-top: 12px; padding: 14px 24px; }

    .nav-overlay {
      display: block;
      position: fixed; inset: 0;
      background: rgba(15,34,51,.72);
      backdrop-filter: blur(4px);
      opacity: 0; visibility: hidden;
      transition: opacity var(--dur-base) var(--ease-out), visibility var(--dur-base);
      z-index: 998;
    }
    body.nav-open .nav-overlay { opacity: 1; visibility: visible; }
    body.nav-open { overflow: hidden; }

    .menu-btn { display: flex; position: relative; z-index: 3; }
  }
  @media (max-width: 768px) {
    .logo img { height: 54px; }
    .nav.scrolled .logo img,
    .nav-solid .logo img { height: 49px; }
    .footer-grid { grid-template-columns: 1fr 1fr; gap: 30px; }
    .footer-brand { grid-column: 1 / -1; }
    .footer-logo img { max-width: 300px; }
    .whatsapp-float { width: 55px; height: 55px; bottom: 20px; right: 20px; }
  }
  @media (max-width: 480px) {
    .nav-container {
      padding-right: max(14px, env(safe-area-inset-right));
      padding-left: max(14px, env(safe-area-inset-left));
    }
    .logo img { height: 49px; }
    .menu-btn { margin-left: 4px; }
    .footer-grid { grid-template-columns: 1fr; gap: 28px; text-align: center; }
    .footer-brand { text-align: center; }
    .footer-links { display: inline-block; text-align: left; }
    .footer-links-2col { column-count: 1; }
    .footer-logo img { max-width: 260px; }
    .footer { padding: 55px 0 30px; }
  }

  /* Mobile Sticky CTA Bar */
  .mobile-cta-bar { display: none; }
  @media (max-width: 768px) {
    .mobile-cta-bar {
      display: flex; position: fixed; bottom: 0; left: 0; width: 100%; z-index: 100;
      background: rgba(15,34,51,0.98); border-top: 1px solid rgba(201,169,97,0.3);
      padding:
        8px
        max(12px, env(safe-area-inset-right))
        calc(8px + env(safe-area-inset-bottom))
        max(12px, env(safe-area-inset-left));
      gap: 10px; backdrop-filter: blur(10px);
    }
    .mobile-cta-btn {
      flex: 1; display: flex; align-items: center; justify-content: center; gap: 8px;
      padding: 12px 10px; border-radius: var(--r-md); font-family: var(--font-sans);
      font-size: 0.9rem; font-weight: 600; text-decoration: none; transition: all 0.2s;
    }
    .mobile-cta-call { background: transparent; color: var(--gold); border: 1.5px solid var(--gold); }
    .mobile-cta-call:active { background: rgba(201,169,97,0.15); }
    .mobile-cta-wa { background: #0E8039; color: #fff; border: none; }
    .mobile-cta-wa:active { background: #1ebe5a; }
    .mobile-cta-btn svg { flex-shrink: 0; }
    .whatsapp-float { display: none !important; }
    body { padding-bottom: calc(65px + env(safe-area-inset-bottom)); }
  }
</style>
